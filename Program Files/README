== main

This program provides the user with a text menu interface through which they can control git.
The program consists of 3 distinct parts.
• A setup wizard.
• A configuration editor.
• A branch/commit exploration tool.




== Build instructions etc.

Type

$ make

in the directory containing this README file. This will create a new file
containing the java byte code. The command

$ ls -lt

will list all files contained in this directory, ordered by modification time.
note the newly created file "example".

== Usage

Run the program with

$ ./main
